# Retired

`mechanistic_validity_unversioned.tex` is the draft that preceded `v23`. It is
identical to v23 except that v23 carries the criterion audits in the same
document and stamps the version on the title page.

**The live paper is `../mechanistic_validity_v23.tex`.** The version series runs
v3 through v22 in the predecessor repository (`mechanistic-validity`, under
`docs/paper/`), and continues here at v23.

Do not edit anything in this directory.
